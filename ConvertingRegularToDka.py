from RegularExpression import *


class ConvertingRegularToDka:

    def __init__(self, regular=''):
        regular = ''
        if self.check_regular_format(regular):
            self.regular = regular

    def check_regular_format(self, reular) -> bool:
        pass
